start
method wordCounter(paragraph)
    words = split paragraph into words (remove punctuation, to lowercase)
    for each word in words:
        if word in map
            map[word] += 1
        else
            map[word] = 1
    sort map by frequency
    display word:count
end