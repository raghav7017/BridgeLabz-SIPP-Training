function wordCounter(articleText):
    words = split articleText into lowercase words, remove punctuation
    map = empty dictionary

    for word in words:
        if word in map:
            map[word] += 1
        else:
            map[word] = 1

    sort map by value (optional)
    print each word and count
